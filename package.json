{
  "name": "duckduckgo-html",
  "version": "1.0.1",
  "description": "",
  "scripts": {
    "lint": "prettier -c . && web-ext lint -w -s src",
    "format": "prettier -w .",
    "build": "web-ext build -o -s src",
    "start": "web-ext run -s src",
    "sign": "web-ext sign -s src"
  },
  "author": "Jordan Doyle <jordan@doyle.la>",
  "license": "WTFPL",
  "devDependencies": {
    "prettier": "^3.0.3",
    "web-ext": "^7.7.0"
  }
}
